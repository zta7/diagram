export function Text({ data }: any = {}) {
  // const onChange = useCallback((evt) => {
  //   console.log(evt.target.value);
  // }, []);
  // console.log('text');
  return (
    <div>
      123
    </div>
  );
}
